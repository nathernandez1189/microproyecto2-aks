apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base]
patches:
  - target: {kind: PersistentVolumeClaim, name: planner-data}
    patch: |-
      - op: add
        path: /spec/storageClassName
        value: managed-csi
